<div class="modal fade" id="itinarary_modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">New message</h5>
        <%= @products %>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <%= bootstrap_form_tag url: create_itinerary_products_itineraries_path do |f| %>
        <div class="modal-body">
            <div class="form-group">
              
              <%= f.hidden_field 'itinerary_product[product_id][]', value: @products.join(',') %>
              <%= f.select 'itinerary_product[itinerary_schedule_id]', @itineraries.all.collect { |x| [x.name, x.id.to_i]}, {label: "Select Itinarary" ,prompt: 'Please select'}, id: 'itinerary_schedule_id' %>
            </div>
            <div class="form-group duration">
              <div class="duration_select">
              </div>
            </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <%= f.submit %>
        </div>
        <% end %>
    </div>
  </div>
</div>

<script type="text/javascript">
  $(document).ready(function(){
    $('#itinerary_schedule_id').change(function() {
   $('.duration_select').empty();
    var id = $("#itinerary_schedule_id option:selected").val();
    $.ajax({
      url: '<%= get_itinerary_schedule_itineraries_path %>',
      type: 'GET',
      data: {itinerary: id},
      dataType: 'JSON'
    }).done(function(data){
      var select_option = []
      for(i=0; i <data["itinerary_schedules"].length; i++)
      {
        console.log(data["itinerary_schedules"][i].id)
        select_option.push('<option value='+ data["itinerary_schedules"][i].id +'> Day ' + (i+1) + '</option>')
      }
      var select_div = '<select class="form-control">'+select_option+'</select>'
      $(".duration_select").append(select_div);
    });
  });  
}); 
</script>